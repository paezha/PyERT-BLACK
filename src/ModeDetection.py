class ModeDetection:
    def __init__(self, processedData=None):
        processedData = processedData
        newData = self.detectModes(processedData)
        self.epsiodeData = newData

    def detectModes(self, processedData):
        newData = None
        return newData

    def getEpisodeData(self):
        return self.episodeData
